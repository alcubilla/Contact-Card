import React, {Component} from "react";

const AddContact =(props) => {

    return(
        <>
        <button onClick={props.message}>Click</button>
        </>   
     );
    }
export default AddContact;